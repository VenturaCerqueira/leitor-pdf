# Leitor de PDF de Servidores – Prefeitura de Riachão do Jacuípe

Este projeto extrai nomes e CPFs de servidores públicos a partir de um arquivo PDF, gerando uma planilha Excel com os dados extraídos e um log das linhas ignoradas.

## Como funciona

- O script lê o PDF informado na variável `pdf_path`.
- Para cada linha do PDF, tenta extrair o nome e o CPF do servidor.
- Os dados extraídos são salvos em um arquivo Excel.
- As linhas que não contêm um CPF válido são registradas em um arquivo de log.

## Requisitos

- Python 3.8 ou superior
- [pdfplumber](https://pypi.org/project/pdfplumber/)
- [pandas](https://pypi.org/project/pandas/)

## Instalação

```sh
pip install pdfplumber pandas
```

## Uso

1. Coloque o arquivo PDF na mesma pasta do script e ajuste o nome do arquivo na variável `pdf_path` se necessário.
2. Execute o script:

```sh
python main.py
```

3. Os arquivos de saída serão gerados na mesma pasta:
   - `servidores_riachao_cpf_nome.xlsx`: Planilha com Nome e CPF dos servidores.
   - `linhas_ignoradas.txt`: Log das linhas do PDF que não foram processadas.

## Estrutura do Projeto

- `main.py`: Script principal de extração.
- `80B72592-59B6-4E02-A0BA-39B9D1B2F7A2.pdf`: PDF de entrada (exemplo).
- `servidores_riachao_cpf_nome.xlsx`: Gerado após execução, contém os dados extraídos.
- `linhas_ignoradas.txt`: Gerado após execução, contém as linhas ignoradas.

## Licença

Anderson Ventura

---